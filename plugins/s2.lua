local function run(msg, matches)
     if matches[1] == 'ای' then
                 
               
       end
     end
    return send_document(org_chat_id,"./icons/7.webp", ok_cb, false))
end
return {         
    patterns = {
    "^(ای)$"
  },
  run = run
}

